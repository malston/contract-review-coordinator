"""Contract-review coordinator -- CCA Domain 1 build exercise.

A multi-agent contract-review system whose `send_email` tool is gated on the
*real completion state* of the risk review, never on the model's narration.
"""
